import logo from "../../asset/img/logo.svg";

const Header = () => {
    return (
        <header className="d-flex">
            <img src={logo} alt="" />
        </header>
    );
}

export default Header;